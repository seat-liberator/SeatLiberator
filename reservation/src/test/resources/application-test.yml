spring:
  datasource:
    url: jdbc:tc:postgresql://localhost:5432/reservation_test
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
      dialect: org.hibernate.dialect.PostgreSQLDialect
