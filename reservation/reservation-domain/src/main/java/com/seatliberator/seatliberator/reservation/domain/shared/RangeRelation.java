package com.seatliberator.seatliberator.reservation.domain.shared;

public enum RangeRelation {
    SAME,
    CONTAINS,
    CONTAINED_BY,
    BEFORE,
    AFTER,
    OVERLAPS_AFTER,
    OVERLAPS_BEFORE
}
